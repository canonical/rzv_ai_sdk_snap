diff --git a/Q01_footfall_counter/src/main.cpp b/Q01_footfall_counter/src/main.cpp
index b27937d..0b580dd 100644
--- a/Q01_footfall_counter/src/main.cpp
+++ b/Q01_footfall_counter/src/main.cpp
@@ -55,6 +55,8 @@
 
 #include "draw.h"
 
+#define DISABLE_AI
+
 /*****************************************
 * Global Variables
 ******************************************/
@@ -1228,23 +1230,26 @@ void mipi_cam_init(void)
 {
     int ret = 0;
     std::cout << "[INFO] MIPI CAM Init \n";
-    const char *commands[4] =
+
+    constexpr size_t NUM_COMMANDS = 5U;
+    std::array<std::string, NUM_COMMANDS> const commands =
     {
-        "media-ctl -d /dev/media0 -r",
-        "media-ctl -d /dev/media0 -V \"\'ov5645 0-003c\':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
-        "media-ctl -d /dev/media0 -l \"\'rzg2l_csi2 10830400.csi2\':1 -> \'CRU output\':0 [1]\"",
-        "media-ctl -d /dev/media0 -V \"\'rzg2l_csi2 10830400.csi2\':1 [fmt:UYVY8_2X8/640x480 field:none]\""
+      "media-ctl -d /dev/media0 -r",
+      "media-ctl -d /dev/media0 -l \"'csi-10830400.csi2':1 -> 'cru-ip-10830000.video':0 [1]\"",
+      "media-ctl -d /dev/media0 -V \"'csi-10830400.csi2':1 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'ov5645 0-003c':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'cru-ip-10830000.video':0 [fmt:UYVY8_2X8/640x480 field:none]\""
     };
 
     /* media-ctl command */
-    for (int i = 0; i < 4; i++)
+    for (auto const& command : commands)
     {
-        std::cout << commands[i] << "\n";
-        ret = system(commands[i]);
+        std::cout << command << "\n";
+        ret = system(command.c_str());
         std::cout << "system ret = " << ret << "\n";
         if (ret < 0)
         {
-            std::cout << "[ERROR]" << __func__ << ": failed media-ctl commands. index = " << i << "\n";
+            std::cout << "[ERROR]" << __func__ << ": failed media-ctl command:" << command << "\n";
             return;
         }
     }
@@ -1327,9 +1332,11 @@ int32_t main(int32_t argc, char * argv[])
 
     /*Disable OpenCV Accelerator due to the use of multithreading */
     #ifdef V2H
+#ifndef DISABLE_AI
     unsigned long OCA_list[16];
     for(int i = 0; i < 16; i++) OCA_list[i] = 0;
     OCA_Activate(&OCA_list[0]);
+#endif
     #endif
 
     /*Multithreading Variables*/
@@ -1342,7 +1349,7 @@ int32_t main(int32_t argc, char * argv[])
     InOutDataType input_data_type;
     bool runtime_status = false;
     std::string gstreamer_pipeline;
-    
+
     if (argc < 2) 
     {
         std::cout << "[ERROR] Please specify Input Source\n";
@@ -1373,8 +1380,7 @@ int32_t main(int32_t argc, char * argv[])
             std::cout << "[INFO] MIPI CAMERA \n";
             mipi_cam_init();
             std::string media_port = query_device_status("CRU");
-            gstreamer_pipeline = "v4l2src device=" + media_port + " ! videoconvert ! appsink";
-
+            gstreamer_pipeline = "v4l2src device=" + media_port + " ! video/x-raw,format=UYVY,width=640,height=480,framerate=15/1 ! videoconvert ! appsink";
         }
         break;
         default:
@@ -1434,6 +1440,7 @@ int32_t main(int32_t argc, char * argv[])
     }
     data_read();
 
+#ifndef DISABLE_AI
     int drpai_fd = open("/dev/drpai0", O_RDWR);
     if (0 > drpai_fd)
     {
@@ -1443,6 +1450,7 @@ int32_t main(int32_t argc, char * argv[])
     }
     /* Set drpai mem start address */
     uint64_t drpaimem_addr_start = 0;
+#endif
 
     /*Load Label from label_list file*/
     label_file_map = load_label_file(label_list);
@@ -1453,6 +1461,7 @@ int32_t main(int32_t argc, char * argv[])
         goto end_main;
     }
 
+#ifndef DISABLE_AI
     /*Load model_dir structure and its weight to runtime object */
     drpaimem_addr_start = init_drpai(drpai_fd);
    
@@ -1487,6 +1496,7 @@ int32_t main(int32_t argc, char * argv[])
         fprintf(stderr, "[ERROR] Input data type : neither FP32 nor FP16.\n");
         goto end_main;
     }
+#endif
 
     /*Termination Request Semaphore Initialization*/
     /*Initialized value at 1.*/
@@ -1498,6 +1508,7 @@ int32_t main(int32_t argc, char * argv[])
         goto end_threads;
     }
 
+#ifndef DISABLE_AI
     /*Create Inference Thread*/
     create_thread_ai = pthread_create(&ai_inf_thread, NULL, R_Inf_Thread, NULL);
     if (0 != create_thread_ai)
@@ -1507,6 +1518,7 @@ int32_t main(int32_t argc, char * argv[])
         ret_main = -1;
         goto end_threads;
     }
+#endif
 
     /* Create Capture Thread */
     create_thread_capture = pthread_create(&capture_thread, NULL, R_Capture_Thread, (void *) &gstreamer_pipeline);
@@ -1546,6 +1558,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#ifndef DISABLE_AI
     if (0 == create_thread_ai)
     {
         ret = wait_join(&ai_inf_thread, AI_THREAD_TIMEOUT);
@@ -1555,6 +1568,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#endif
     if (0 == create_thread_key)
     {
         ret = wait_join(&kbhit_thread, EXIT_THREAD_TIMEOUT);
@@ -1570,6 +1584,7 @@ end_threads:
     {
         sem_destroy(&terminate_req_sem);
     }
+#ifndef DISABLE_AI
     goto end_close_drpai;
 end_close_drpai:
     /*Close DRP-AI Driver.*/
@@ -1583,6 +1598,7 @@ end_close_drpai:
             ret_main = -1;
         }
     }
+#endif
     goto end_main;
 
 end_main:
diff --git a/Q08_object_counter/src/main.cpp b/Q08_object_counter/src/main.cpp
index 6dd1f12..c217cac 100755
--- a/Q08_object_counter/src/main.cpp
+++ b/Q08_object_counter/src/main.cpp
@@ -51,6 +51,8 @@
 #include "box.h"
 #include "wayland.h"
 
+#define DISABLE_AI
+
 /*Multithreading*/
 static sem_t terminate_req_sem;
 static pthread_t ai_inf_thread;
@@ -1228,22 +1230,26 @@ void mipi_cam_init(void)
 {
     int ret = 0;
     std::cout << "[INFO] MIPI CAM Init \n";
-    const char *commands[4] =
-        {
-            "media-ctl -d /dev/media0 -r",
-            "media-ctl -d /dev/media0 -V \"\'ov5645 0-003c\':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
-            "media-ctl -d /dev/media0 -l \"\'rzg2l_csi2 10830400.csi2\':1 -> \'CRU output\':0 [1]\"",
-            "media-ctl -d /dev/media0 -V \"\'rzg2l_csi2 10830400.csi2\':1 [fmt:UYVY8_2X8/640x480 field:none]\""};
+
+    constexpr size_t NUM_COMMANDS = 5U;
+    std::array<std::string, NUM_COMMANDS> const commands =
+    {
+      "media-ctl -d /dev/media0 -r",
+      "media-ctl -d /dev/media0 -l \"'csi-10830400.csi2':1 -> 'cru-ip-10830000.video':0 [1]\"",
+      "media-ctl -d /dev/media0 -V \"'csi-10830400.csi2':1 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'ov5645 0-003c':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'cru-ip-10830000.video':0 [fmt:UYVY8_2X8/640x480 field:none]\""
+    };
 
     /* media-ctl command */
-    for (int i = 0; i < 4; i++)
+    for (auto const& command : commands)
     {
-        std::cout << commands[i] << "\n";
-        ret = system(commands[i]);
+        std::cout << command << "\n";
+        ret = system(command.c_str());
         std::cout << "system ret = " << ret << "\n";
         if (ret < 0)
         {
-            std::cout << "[ERROR]" << __func__ << ": failed media-ctl commands. index = " << i << "\n";
+            std::cout << "[ERROR]" << __func__ << ": failed media-ctl command:" << command << "\n";
             return;
         }
     }
@@ -1266,11 +1272,13 @@ int32_t main(int32_t argc, char *argv[])
     int8_t ret_main = 0;
 
 #ifdef V2H
+#ifndef DISABLE_AI
     /*Disable OpenCV Accelerator due to the use of multithreading */
     unsigned long OCA_list[16];
     for (int i = 0; i < 16; i++)
         OCA_list[i] = 0;
     OCA_Activate(&OCA_list[0]);
+#endif
 #endif
 
     /*Multithreading Variables*/
@@ -1292,7 +1300,6 @@ int32_t main(int32_t argc, char *argv[])
 
     std::string mode = argv[1];
     std::string input_source = argv[2];
-
     std::map<std::string, std::string> args;
     /* Parse input arguments */
     for (int i = 1; i < argc; ++i)
@@ -1358,7 +1365,7 @@ int32_t main(int32_t argc, char *argv[])
         std::cout << "[INFO] MIPI CAMERA \n";
         mipi_cam_init();
         std::string media_port = query_device_status("CRU");
-        gstreamer_pipeline = "v4l2src device=" + media_port + " ! videoconvert ! appsink";
+        gstreamer_pipeline = "v4l2src device=" + media_port + " ! video/x-raw,format=UYVY,width=640,height=480,framerate=15/1 ! videoconvert ! appsink";
     }
     break;
     /* Input Source : USB Camera */
@@ -1378,6 +1385,7 @@ int32_t main(int32_t argc, char *argv[])
     }
     }
 
+#ifndef DISABLE_AI
     int drpai_fd = open("/dev/drpai0", O_RDWR);
     if (0 > drpai_fd)
     {
@@ -1386,6 +1394,7 @@ int32_t main(int32_t argc, char *argv[])
     }
 
     uint64_t drpaimem_addr_start = 0;
+#endif
 
     /*Load Label from label_list file*/
     int emptyCount = 0;
@@ -1417,6 +1426,7 @@ int32_t main(int32_t argc, char *argv[])
 
     drpai_output_buf = new float[INF_OUT_SIZE];
 
+#ifndef DISABLE_AI
     /*Load model_dir structure and its weight to runtime object */
     drpaimem_addr_start = init_drpai(drpai_fd);
 
@@ -1450,6 +1460,7 @@ int32_t main(int32_t argc, char *argv[])
         fprintf(stderr, "[ERROR] Input data type : neither FP32 nor FP16.\n");
         goto end_main;
     }
+#endif
 
     /*Termination Request Semaphore Initialization*/
     /*Initialized value at 1.*/
@@ -1470,6 +1481,7 @@ int32_t main(int32_t argc, char *argv[])
         goto end_threads;
     }
 
+#ifndef DISABLE_AI
     /*Create Inference Thread*/
     create_thread_ai = pthread_create(&ai_inf_thread, NULL, R_Inf_Thread, NULL);
     if (0 != create_thread_ai)
@@ -1479,6 +1491,7 @@ int32_t main(int32_t argc, char *argv[])
         ret_main = -1;
         goto end_threads;
     }
+#endif
 
     /*Create Capture Thread*/
     create_thread_capture = pthread_create(&capture_thread, NULL, R_Capture_Thread, (void *)&gstreamer_pipeline);
@@ -1509,6 +1522,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#ifndef DISABLE_AI
     if (0 == create_thread_ai)
     {
         ret = wait_join(&ai_inf_thread, AI_THREAD_TIMEOUT);
@@ -1518,6 +1532,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#endif
     if (0 == create_thread_key)
     {
         ret = wait_join(&kbhit_thread, EXIT_THREAD_TIMEOUT);
@@ -1535,6 +1550,7 @@ end_threads:
     }
     goto end_main;
 end_main:
+#ifndef DISABLE_AI
     /*Close DRP-AI Driver.*/
     if (0 < drpai_fd)
     {
@@ -1546,6 +1562,7 @@ end_main:
             ret_main = -1;
         }
     }
+#endif
     delete[] drpai_output_buf;
     printf("Application End\n");
     return ret_main;
diff --git a/R01_object_detection/src/main.cpp b/R01_object_detection/src/main.cpp
index 3d309c6..3b86be3 100755
--- a/R01_object_detection/src/main.cpp
+++ b/R01_object_detection/src/main.cpp
@@ -66,6 +66,8 @@
 /*Mutual exclusion*/
 #include <mutex>
 
+#define DISABLE_AI
+
 /*****************************************
 * Global Variables
 ******************************************/
@@ -650,24 +652,28 @@ int8_t print_result(Image* img, cv::Mat& proc_image)
 #ifndef V2H /* V2L */
 void mipi_cam_init(void)
 {
-    int8_t ret = 0;
-    const char* commands[4] =
+    int ret = 0;
+    std::cout << "[INFO] MIPI CAM Init \n";
+
+    constexpr size_t NUM_COMMANDS = 5U;
+    std::array<std::string, NUM_COMMANDS> const commands =
     {
-        "media-ctl -d /dev/media0 -r",
-        "media-ctl -d /dev/media0 -V \"\'ov5645 0-003c\':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
-        "media-ctl -d /dev/media0 -l \"\'rzg2l_csi2 10830400.csi2\':1 -> \'CRU output\':0 [1]\"",
-        "media-ctl -d /dev/media0 -V \"\'rzg2l_csi2 10830400.csi2\':1 [fmt:UYVY8_2X8/640x480 field:none]\""
+      "media-ctl -d /dev/media0 -r",
+      "media-ctl -d /dev/media0 -l \"'csi-10830400.csi2':1 -> 'cru-ip-10830000.video':0 [1]\"",
+      "media-ctl -d /dev/media0 -V \"'csi-10830400.csi2':1 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'ov5645 0-003c':0 [fmt:UYVY8_2X8/640x480 field:none]\"",
+      "media-ctl -d /dev/media0 -V \"'cru-ip-10830000.video':0 [fmt:UYVY8_2X8/640x480 field:none]\""
     };
 
     /* media-ctl command */
-    for (int i=0; i<4; i++)
+    for (auto const& command : commands)
     {
-        printf("%s\n", commands[i]);
-        ret = system(commands[i]);
-        printf("system ret = %d\n", ret);
-        if (ret<0)
+        std::cout << command << "\n";
+        ret = system(command.c_str());
+        std::cout << "system ret = " << ret << "\n";
+        if (ret < 0)
         {
-            printf("%s: failed media-ctl commands. index = %d\n", __func__, i);
+            std::cout << "[ERROR]" << __func__ << ": failed media-ctl command:" << command << "\n";
             return;
         }
     }
@@ -936,6 +942,7 @@ void *R_Capture_Thread(void *threadid)
                     /*Add padding for keeping the aspect ratio: CAM_IMAGE_WIDTH*CAM_IMAGE_WIDTH (BGR) */
                     cv::vconcat(input_image, padding_frame, input_image);
                     
+#ifndef DISABLE_AI
                     /*Copy input data to drpai_buf for DRP-AI Pre-processing Runtime.*/
                     memcpy( drpai_buf->mem, input_image.data, drpai_buf->size);
                     /* Flush buffer */
@@ -944,6 +951,7 @@ void *R_Capture_Thread(void *threadid)
                     {
                         goto err;
                     }
+#endif
                     mtx.unlock();
                     inference_start.store(1); /* Flag for AI Inference Thread. */
                 }
@@ -1357,6 +1365,7 @@ int32_t main(int32_t argc, char * argv[])
     }
 
     #ifdef V2H
+#ifndef DISABLE_AI
     /*Disable OpenCV Accelerator due to the use of multithreading */
     #ifdef V2N
     unsigned long OCA_list[16];
@@ -1366,6 +1375,7 @@ int32_t main(int32_t argc, char * argv[])
     for (int i=0; i < OCA_LIST_NUM; i++) OCA_list[i] = 0;
     #endif
     OCA_Activate( &OCA_list[0] );
+#endif
     printf("Disable OpenCVA\n");
     
     std::string media_port = query_device_status("usb");
@@ -1374,8 +1384,11 @@ int32_t main(int32_t argc, char * argv[])
     /* MIPI Camera Setup */
     mipi_cam_init();
     #endif
-    gstreamer_pipeline = "v4l2src device=" + media_port +" io-mode=dmabuf ! video/x-raw, width="+std::to_string(CAM_IMAGE_WIDTH)+", height="+std::to_string(CAM_IMAGE_HEIGHT)+" ,framerate=30/1 ! videoconvert ! appsink -v";
-
+#ifndef DISABLE_AI
+    gstreamer_pipeline = "v4l2src device=" + media_port + " io-mode=dmabuf ! video/x-raw,format=UYVY,width="+std::to_string(CAM_IMAGE_WIDTH)+", height="+std::to_string(CAM_IMAGE_HEIGHT)+" ,framerate=30/1 ! videoconvert ! appsink -v";
+#else
+    gstreamer_pipeline = "v4l2src device=" + media_port + " io-mode=dmabuf ! videoconvert ! appsink -v";
+#endif
     printf("RZ/V AI SDK Sample Application\n");
     printf("Model : Darknet YOLOv3 | %s\n", model_dir.c_str());
     printf("Input : %s\n", INPUT_CAM_NAME);
@@ -1411,6 +1424,7 @@ int32_t main(int32_t argc, char * argv[])
 
     /*DRP-AI Driver initialization*/
     errno = 0;
+#ifndef DISABLE_AI
     drpai_fd = open("/dev/drpai0", O_RDWR);
     if (0 > drpai_fd)
     {
@@ -1474,6 +1488,7 @@ int32_t main(int32_t argc, char * argv[])
         fprintf(stderr, "[ERROR] Failed to Allocate DMA buffer for the drpai_buf\n");
         goto end_free_malloc;
     }
+#endif
 
     /*Initialize Image object.*/
     ret = img.init(CAM_IMAGE_WIDTH, CAM_IMAGE_HEIGHT, CAM_IMAGE_CHANNEL_BGR, 
@@ -1503,6 +1518,7 @@ int32_t main(int32_t argc, char * argv[])
         ret_main = -1;
         goto end_threads;
     }
+#ifndef DISABLE_AI
     /*Create Inference Thread*/
     create_thread_ai = pthread_create(&ai_inf_thread, NULL, R_Inf_Thread, NULL);
     if (0 != create_thread_ai)
@@ -1512,6 +1528,7 @@ int32_t main(int32_t argc, char * argv[])
         ret_main = -1;
         goto end_threads;
     }
+#endif
     /*Create Capture Thread*/
     create_thread_capture = pthread_create(&capture_thread, NULL, R_Capture_Thread, NULL);
     if (0 != create_thread_capture)
@@ -1586,6 +1603,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#ifndef DISABLE_AI
     if (0 == create_thread_ai)
     {
         ret = wait_join(&ai_inf_thread, AI_THREAD_TIMEOUT);
@@ -1595,6 +1613,7 @@ end_threads:
             ret_main = -1;
         }
     }
+#endif
     if (0 == create_thread_key)
     {
         ret = wait_join(&kbhit_thread, KEY_THREAD_TIMEOUT);
@@ -1616,16 +1635,21 @@ end_threads:
     goto end_close_dmabuf;
 
 end_close_dmabuf:
+#ifndef DISABLE_AI
     buffer_free_dmabuf(drpai_buf);
+#endif
     goto end_free_malloc;
 
 end_free_malloc:
+#ifndef DISABLE_AI
     free(drpai_buf);
+#endif
     drpai_buf = NULL;
     
     goto end_close_drpai;
 
 end_close_drpai:
+#ifndef DISABLE_AI
     /*Close DRP-AI Driver.*/
     if (0 < drpai_fd)
     {
@@ -1637,6 +1661,7 @@ end_close_drpai:
             ret_main = -1;
         }
     }
+#endif
     goto end_main;
 
 end_main:
